using System;

class TESTE {
    static void Main () {
        var N = int.Parse(Console.ReadLine());
        var M = int.Parse(Console.ReadLine());
        
        if (N >= 1 && N <= 40 && M >= 0 && M <= 40);
        {
          int dif = N - M;
          Console.WriteLine(dif);
        }//Termine o código 
        
    }
}